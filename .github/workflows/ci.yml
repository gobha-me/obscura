name: CI

# Enforces the AGENTS.md rule: "build on both compilers, always." Every push to
# main and every pull request builds + tests the project on GCC and Clang across
# the default toolchain and each sanitizer, so a change that only builds on one
# compiler fails visibly.
#
# Inherited from the cpp-template starter kit and kept close to its reference
# form: it hardcodes nothing project-specific, and the project name is derived
# from the checkout directory (see root CMakeLists.txt). The two consumer
# harness jobs it shipped with are gone — OBSCURA is an application, and the
# example/ projects they ran were deleted at bootstrap.

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format:
    name: Clang Format 20
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Install Clang Format 20
        env:
          CLANG_VERSION: "20"
        run: |
          wget -qO /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          sudo /tmp/llvm.sh "$CLANG_VERSION"
          sudo apt-get install -y "clang-format-$CLANG_VERSION"

      - name: Self-test source discovery
        run: tools/format.sh --selftest

      - name: Check first-party C++ formatting
        run: tools/format.sh --check

  clang-tidy:
    name: Clang Tidy 20
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Clang Tidy 20
        env:
          CLANG_VERSION: "20"
        run: |
          wget -qO /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          sudo /tmp/llvm.sh "$CLANG_VERSION"
          sudo apt-get install -y "clang-tidy-$CLANG_VERSION" "clang-tools-$CLANG_VERSION"

      - name: Check first-party C++
        run: tools/lint.sh

  build:
    name: ${{ matrix.name }}
    runs-on: ubuntu-24.04   # ships GCC 13 + Clang (both C++23-capable); pinned for reproducibility
    strategy:
      fail-fast: false      # always show every compiler's result, not just the first failure
      matrix:
        include:
          - { name: "gcc / default",   cxx: g++,     preset: default }
          - { name: "gcc / asan",      cxx: g++,     preset: asan }
          - { name: "gcc / tsan",      cxx: g++,     preset: tsan }
          - { name: "gcc / ubsan",     cxx: g++,     preset: ubsan }
          - { name: "clang / default", cxx: clang++, preset: clang }
          - { name: "clang / asan",    cxx: clang++, preset: asan }
          - { name: "clang / tsan",    cxx: clang++, preset: tsan }
          - { name: "clang / ubsan",   cxx: clang++, preset: ubsan }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0    # full history + tags so cmake/version.cmake's `git describe --tags` resolves a real version

      # GCC jobs use the preinstalled g++ (13). The clang jobs, however, must NOT
      # use ubuntu-24.04's stock clang 18: its libstdc++ pairing cannot compile
      # the C++23 std::expected example (test/20failure-testing) — the same class
      # of compiler/stdlib break as the documented fmt-under-clang-20 issue. Pin
      # them to a known-good Clang so "both compilers, always" stays honest. The
      # symlinks make `clang++` resolve to the pinned version, so clang.cmake's
      # find_program(clang++) and the sanitizer toolchains' $CXX all pick it up.
      - name: Install Clang
        if: ${{ startsWith(matrix.cxx, 'clang') }}
        env:
          CLANG_VERSION: "20"
        run: |
          wget -qO /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          sudo /tmp/llvm.sh "$CLANG_VERSION"
          sudo ln -sf "/usr/bin/clang-$CLANG_VERSION"   /usr/local/bin/clang
          sudo ln -sf "/usr/bin/clang++-$CLANG_VERSION" /usr/local/bin/clang++
          clang++ --version

      - name: Configure
        env:
          CXX: ${{ matrix.cxx }}   # default/sanitizer toolchains respect $CXX; clang.cmake selects clang itself
        run: cmake --preset "${{ matrix.preset }}"

      # --parallel 2, not bare --parallel. This is a deliberate deviation from
      # cpp-template's "copy ci.yml verbatim" rule, and it is load-bearing here:
      # bare --parallel means nproc, which is 4 on a standard runner, and four
      # concurrent GCC compilations of Catch2's translation units plus TermForge
      # exhaust the runner. It fails as "The runner has received a shutdown
      # signal" / exit 143 partway through Catch2 — which reads like flaky
      # infrastructure rather than resource exhaustion, and cost an afternoon to
      # attribute. Clang survives it; GCC does not. The build presets carry this
      # limit so local and CI invocations exercise the same resource contract.
      #
      # The template does not hit this because it fetches only Catch2. OBSCURA
      # adds TermForge, which roughly doubles the compile load.
      - name: Build
        run: cmake --build --preset "${{ matrix.preset }}"

      - name: Test
        # Runs the Catch2 suites, the sanitizer engagement smoke test, and the
        # CT-05 version-parse-selftest.
        run: ctest --preset "${{ matrix.preset }}"

  # The library-disabled path, which the matrix above never exercises: every job
  # there builds with the library ON, so nothing would catch a regression in the
  # `if (TARGET ${PROJECT_NAME}::lib)` guards that keep -D<name>_BUILD_LIB=OFF a
  # supported configuration. OFF removes the target outright, so the tests that
  # call into the library cannot link — hence the documented pairing with
  # -D<name>_TESTS=OFF, which is exactly what this job proves still works.
  #
  # It also installs, which it did not use to: building alone left the *install*
  # side of the library-disabled path uncovered, and that is exactly where #33
  # lived — the package-config rules ran unguarded, so this configuration
  # deposited a Config.cmake with no Targets.cmake beside it.
  #
  # The project name is derived from the checkout directory, same as the root
  # CMakeLists.txt does, so this stays copyable to a fork verbatim.
  lib-off:
    name: library disabled
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Configure, build, and install with the library off
        run: |
          set -euo pipefail
          NAME=$(basename "$GITHUB_WORKSPACE")
          cmake -B build "-D${NAME}_BUILD_LIB=OFF" "-D${NAME}_TESTS=OFF"
          cmake --build build --parallel 2
          cmake --install build --prefix "$PWD/prefix"

          # With no library there is nothing to export, so the package config
          # must not be installed either: a Config.cmake with no Targets.cmake
          # beside it shadows a good install later on CMAKE_PREFIX_PATH and then
          # sticks in the consumer's <project>_DIR cache, where re-pointing
          # CMAKE_PREFIX_PATH does not dislodge it. cmake/install.cmake carries
          # the reasoning; this is the check that keeps it true.
          #
          # To re-prove this assertion can fail, drop the `if (TARGET
          # ${PROJECT_NAME}_lib)` guard around that file's package-config
          # section and watch the lib/cmake branch go red.
          #
          # The executable check is not decoration. Without it the two absence
          # checks below would pass just as happily against an install that did
          # nothing at all.
          test -x "prefix/bin/${NAME}" || {
            echo "::error::the executable was not installed, so the absence checks below prove nothing"
            ls -R prefix || true
            exit 1
          }
          for unexpected in prefix/lib/cmake prefix/include; do
            if [ -e "${unexpected}" ]; then
              echo "::error::${unexpected} exists, but the library is disabled: a library-less install ships the application only"
              ls -R prefix
              exit 1
            fi
          done

  # Fast, dependency-free guard: the git-describe version parser self-test on its
  # own (no compiler, no network). It also runs inside every build job above via
  # ctest; this job gives quick, isolated feedback.
  version-selftest:
    name: version parser self-test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Run version_selftest.cmake
        run: cmake -P cmake/version_selftest.cmake
